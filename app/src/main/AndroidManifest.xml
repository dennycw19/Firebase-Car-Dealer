<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cardealerproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".jualbelidialog"></activity>
        <activity android:name=".TransaksiBeli" />
        <activity android:name=".About" />
        <activity android:name=".TransaksiList" />
        <activity android:name=".Terjual" />
        <activity android:name=".Transaksi" />
        <activity android:name=".UpdateDialog" />
        <activity android:name=".FormTambahMobil" />
        <activity android:name=".MobilList" />
        <activity android:name=".StokMobil" />
        <activity android:name=".Home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>